import { ImageProps } from "react-native";

export const WATER_SPLASHES:ImageProps = require('../assets/images/water-splashes.png')
export const Circle_Length = 1000;
export const Radius = Circle_Length / (2 * Math.PI);